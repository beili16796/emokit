# Changelog

## 0.1.0 - Initial Release

- Added LOSO-first benchmark framing for DEAP and SEED-V experiments.
- Added YAML/CLI support for `cross_corpus` evaluation with a required
  `target_dataset` block.
- Added SEED to DREAMER cross-corpus example configuration.
- Added YAML example for `FeatureMixup` and `TemporalSegmentPermutation`
  augmentation ablation.
- Added Dockerfile and quickstart examples.
